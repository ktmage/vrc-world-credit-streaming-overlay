# コントリビューションガイド

[English](./CONTRIBUTING.md) | 日本語

このプロジェクトへの改善提案・修正に関心を持っていただきありがとうございます。

これは、現在訪問中の VRChat ワールドのクレジットを OBS ブラウザソースに表示するための非公式ツールです。コントリビューションはこの範囲を保ってください。VRChat へのログイン機能、認証情報の取り扱い、非公開アカウントデータへのアクセス、VRChat の Terms of Service または Creator Guidelines に反する挙動は扱いません。

## 開発環境

依存関係をインストール:

```bash
bun install
```

開発モードで起動:

```bash
VRCHAT_API_CONTACT=you@example.com bun run dev
```

クライアントをビルド:

```bash
bun run build
```

サーバとクライアントを型チェック:

```bash
bunx tsc -p src/server/tsconfig.json --noEmit
bunx tsc -p src/client/tsconfig.json --noEmit
```

## Pull Request の前に

以下を確認してください:

- `bun run build` 後に `bun run start` でアプリが起動できる。
- サーバとクライアントの型チェックが通る。
- README の説明が実際の挙動と一致している。
- ユーザー向けの変更がある場合はドキュメントも更新している。
- 新しい API 挙動が不要なリクエストを増やさず、VRChat API のレート制限に慎重に対応している。

## コード方針

- 変更は小さく、目的を絞る。
- 新しい抽象化よりも、既存のプロジェクトの書き方を優先する。
- サーバ・クライアント共有のデータ契約は `src/schema.ts` に置く。
- ブラウザソース UI はシンプルで OBS で扱いやすい状態を保つ。
- 依存追加は、意味のある複雑さを減らせる場合に限る。
- `.env` や `start.cmd` などの個人用ファイルはコミットしない。

## スタイルプリセット

同梱スタイルプリセットの追加提案は歓迎します。よいプリセットは、OBS でそのまま使いやすく、Custom CSS で上書きしやすく、既存プリセットと見た目の役割がはっきり違うものです。

スタイルプリセットを提案するときは、実際のブラウザソースに近いサイズで見たスクリーンショット、または短いキャプチャを添えてください。トップバー、ローワーサード、コンパクトなバッジ、大きめのクレジットカードなど、想定している使いどころがある場合は Pull Request に書いてください。

## Issue

バグ報告には、可能な範囲で以下を含めてください:

- 期待した挙動。
- 実際に起きた挙動。
- OS と Bun のバージョン。
- 関連する環境変数。ただし非公開値は伏せる。
- 役に立つログやスクリーンショット。

機能要望では、対応したい配信ワークフローを説明してください。希望する実装方法だけよりも、その背景のほうが判断材料になります。

## セキュリティとプライバシー

Issue や Pull Request に、シークレット、VRChat の認証情報、セッショントークン、非公開ログ、非公開ユーザーデータを投稿しないでください。

セキュリティまたはプライバシー上の問題を見つけた場合は、可能であればメンテナーへ非公開で報告してください。非公開の連絡先がない場合は、機密情報を含めず、影響範囲だけを最小限に説明する Issue を作成してください。
